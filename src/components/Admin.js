import React from 'react';

export default class Admin extends React.Component {
    render() {
        return (
            <div>
                Admin will enter a code, then will receive all not approved Quotes and then Admin will be able to update them
            </div>
        )
    }
};